---
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<section class="text-white flex flex-col gap-8 py-4">
  <h2 class="md:text-4xl text-3xl font-semibold">{title}</h2>
  <div class="flex flex-col md:flex-row gap-4">
    <slot />
  </div>
</section>
